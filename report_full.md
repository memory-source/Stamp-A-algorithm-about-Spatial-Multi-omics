# STAMP: 一种图神经网络驱动的空间多组学融合聚类方法
## ——从文献调研到算法设计到 Benchmark 评估的完整 Vibe Coding 实践

---

**作者**: 吕勇稷
**课程**: 空间多组学聚类：设计、实现与 Benchmark
**日期**: 2026年5月

---

# Part A: 算法设计与技术结果

## 1. 调研综述：Graph-based 空间多模态融合前沿

### 1.1 研究背景与挑战

空间多组学（Spatial Multi-Omics）技术能够在保留组织空间上下文的同时，对同一组织切片进行多种分子模态的共检测（如转录组 RNA-seq 与表观组 ATAC-seq）。然而，不同模态在特征维度、数据质量、噪声水平和生物学信号上存在显著差异：RNA 数据维度高但受 dropout 噪声影响大，ATAC 数据极度稀疏（>95% 为零）。如何有效融合这些异质数据以识别精确的空间域（Spatial Domain），是当前计算生物学领域的核心挑战。

基于图神经网络（GNN）的融合方法因其能够显式建模空间邻近关系和特征相似性，已成为该领域的主流范式。本调研系统总结了 graph-based 空间多模态融合的三大技术路线：

**路线一：注意力引导的中期融合**。以 SpatialGlue（Long et al., 2024, *Nature Methods*）为代表，采用双重注意力聚合架构，在每个模态内部并行构建空间邻近图与特征相似性图，通过分层注意力（modality-within + modality-between）实现自适应融合。MultiGATE（Miao et al., 2025）进一步将基因组距离先验显式编码到跨模态注意力中。

**路线二：对比学习对齐**。以 SpaMosaic（Yan et al., 2026, *Nature Genetics*）和 GraphST（Long et al., 2023）为代表，利用 InfoNCE 损失最大化同一 spot 的跨模态嵌入相似度，将空间邻居关系作为"免费"的监督信号。

**路线三：生成式融合**。以 CANDIES 为代表，采用条件扩散 Transformer 处理质量不均衡模态，再通过 GCN 实现跨模态对齐。

### 1.2 关键方法核心架构总结

| 方法 | 核心架构一句话总结 |
|------|-------------------|
| **SpatialGlue** | 基于GNN的双重注意力聚合，显式并行处理空间图与特征图，通过分层注意力实现自适应融合 |
| **SpaDDM** | 方向性扩散模型 + 注意力跨组学对齐，利用物理先验引导模态间信息传递 |
| **STAGATE** | 空间单组学图注意力自编码器，通过空间邻域图聚合实现域识别 |
| **GraphST** | 图自监督对比学习，最小化空间相邻 spot 嵌入距离 |
| **MultiGATE** | 双层图注意力自编码器，将基因组距离先验编码到跨模态注意力 |

### 1.3 空间图构建策略

| 策略 | 机制 | 关键参数 | 适用场景 |
|------|------|---------|---------|
| k-近邻 (kNN) | 保留k个最近空间邻居 | k=6~15 | 通用场景 |
| 距离阈值 | 固定半径r内所有邻居 | r=0.05~0.15（归一化坐标） | 已知生物学作用尺度 |
| 特征相似性图 | 基于低维表示的kNN | k=10~20 | 补充空间图的信息盲区 |

### 1.4 跨模态融合的设计选择 Trade-off

- **早期融合**（拼接后统一建模）：实现简单，但噪声放大、维度灾难
- **中期融合**（隐藏层交互）：SpatialGlue、MultiGATE 采用，平衡灵活性与复杂性
- **晚期融合**（决策层集成）：模态独立性高，但协同信息可能丢失

**注意力引导融合**可解释性强，但注意力权重可能过度集中于主导模态；**对比学习对齐**利用自然共现对作为免费监督，但对负样本构造敏感；**生成式融合**（跨模态重建）能处理质量不均衡模态，但训练不稳定。

---

## 2. STAMP v7a 方法设计

### 2.1 方法名称与核心思路

**STAMP**（**S**patial **T**ranscriptome-**A**TAC **M**ulti-modal **P**airing）v7a 是一种面向空间 RNA+ATAC 数据的图神经网络融合聚类方法。

核心设计思路基于三个观察：
1. **空间图 alone 不足**：空间邻近的 spot 不一定具有相似的特征表达（尤其 RNA 噪声大时），需要补充特征层面的邻居关系
2. **ATAC 质量普遍优于 RNA**：在模拟数据中，ATAC 单模态 ARI 平均达 0.91，而 RNA 仅 0.35，融合策略应显式保护高质量模态
3. **跨模态重建是防止表示坍塌的关键**：消融实验表明，去除交叉重建后 ARI 跌至 0.24

### 2.2 架构设计与数据流

```
RNA (PCA-50d) --→ DualGATEncoder --→ z_r_base --┐
          ↑ (spatial_graph + feature_graph)       │--→ Asymmetric CMA --→ z_r
ATAC (LSI-50d) --→ DualGATEncoder --→ z_a ───────┘
          ↑ (spatial_graph + feature_graph)

z_stamp = concat([z_r, z_a]) --→ Leiden 聚类 --→ 空间域标签
```

**四个核心模块**：

**模块一：双图 GAT 编码器（DualGATEncoder）**——v7a 核心创新

每个模态配备独立的 DualGATEncoder，同时处理两个图：
- **空间图**：基于物理坐标的 kNN（k=6, radius=0.06），编码组织空间结构
- **特征图**：基于 PCA/LSI 低维表示的 kNN（k=15），编码特征相似性
- **可学习融合权重**：两个图通过共享的 GAT 层分别传播，然后在每层后用可学习的 `graph_alpha`（初始值 0.85，sigmoid 后约 0.70）加权融合，偏向空间图

与 SpatialGlue 的区别：SpatialGlue 使用完全独立的双编码器（空间图一个 GNN，特征图一个 GNN），然后在注意力层融合。STAMPv7a 使用**共享权重的 GAT 层**分别处理两个图，参数更高效。

**模块二：非对称跨模态注意力（Asymmetric CMA）**

仅让 RNA 的潜在表示去"查询" ATAC 的信息：
```
z_r = sigmoid(gate) * Attention(Q=z_r_base, K=z_a, V=z_a) + (1-sigmoid(gate)) * z_r_base
```
Sigmoid 门控偏置 2.0，初始保留 88% 原始 RNA 信号。ATAC 表示完全不受 RNA 影响，保持纯净。

**模块三：跨模态重建解码器**

每个模态的潜在表示必须能够重建**两个模态**的输入特征（4 项 MSE 损失），这是防止表示坍塌的必要约束。

**模块四：多目标优化**

```python
loss = loss_recon + loss_l2 + loss_align + loss_spatial + loss_spatial_r + loss_spatial_a
```

| 损失项 | 说明 | 权重 |
|--------|------|------|
| `loss_recon` | 4 项交叉重建 MSE | 1.0 |
| `loss_l2` | 潜在表示 L2 正则 | 1e-3 |
| `loss_align` | Stop-gradient BYOL 风格对齐 | 0.1 |
| `loss_spatial` | 融合表示的空间对比（InfoNCE） | 0.3 |
| `loss_spatial_r/a` | RNA/ATAC 单模态空间对比 | 0.1 |

### 2.3 与 SpatialGlue / SpaDDM 的核心区别

| 维度 | SpatialGlue | SpaDDM | STAMP v7a |
|------|-------------|--------|-----------|
| **融合方向** | 双向对称注意力 | 扩散模型+注意力 | **非对称单向**（RNA←ATAC） |
| **空间图策略** | 双独立编码器 | 方向性扩散 | **双图共享权重编码器** |
| **核心约束** | 注意力权重可解释性 | 物理先验引导 | **跨模态重建循环一致性** |
| **模态均衡假设** | 假设模态质量相当 | 假设模态质量相当 | **显式保护高质量模态** |
| **重建机制** | 无显式重建约束 | 无重建约束 | **强制双向跨模态重建** |

---

## 3. 实现细节

### 3.1 网络结构参数

| 组件 | 参数配置 |
|------|---------|
| **DualGATEncoder (RNA)** | 2-layer GATConv: 50→128→30; heads=8; BatchNorm; ELU; 残差连接 |
| **DualGATEncoder (ATAC)** | 2-layer GATConv: 50→128→30; heads=4; BatchNorm; ELU; 残差连接 |
| **CMA 层** | Sigmoid 门控残差注意力; Query=RNA, Key/Value=ATAC |
| **解码器** | 2-layer MLP: 30→128→50; 4 个独立实例 |
| **融合表示** | `concat([z_r, z_a])` → 60 维 |

### 3.2 图构建参数

| 图类型 | 构建方式 | 参数 |
|--------|---------|------|
| 空间图 | 归一化坐标上的 kNN + 距离阈值 | k=6, radius=0.06 |
| RNA 特征图 | PCA-50d 空间上的 kNN | k=15 |
| ATAC 特征图 | LSI-50d 空间上的 kNN | k=15 |

### 3.3 训练超参数

| 参数 | 值 |
|------|-----|
| 优化器 | Adam (lr=1e-3, weight_decay=1e-4) |
| 学习率调度 | CosineAnnealingLR (T_max=500, eta_min=1e-5) |
| 最大 Epoch | 1500 |
| 早停耐心 | 200 epochs |
| 随机种子 | 42 |

### 3.4 后处理流程

与所有 baseline 保持一致以确保公平对比：
1. 空间平滑：0/1/2 轮邻居平均平滑
2. PCA 降维：维度选择 [60, 20, 15, 10]
3. Leiden 聚类：分辨率搜索 [0.05, ..., 0.4]
4. 以 ARI 最大化为目标选择最优参数组合

---

## 4. Benchmark 结果

### 4.1 数据集

使用 Fig2_Benchmark 中的 5 个模拟数据集（Simulated_Dataset_1~5），每个数据集包含配对的 RNA + ATAC 数据以及 ground truth 细胞类型标注。

### 4.2 ARI 结果表

| 方法 | D1 | D2 | D3 | D4 | D5 | **Mean ARI** |
|------|-----|-----|-----|-----|-----|-------------|
| **STARNet** | — | 0.990 | **0.937** | **0.979** | **0.980** | **0.971** |
| **SpatialGlue** | **0.882** | **0.960** | 0.874 | 0.978 | **0.986** | 0.936 |
| STmultiGRN | **0.913** | — | — | — | — | 0.913 |
| **STAMPv7a (ours)** | **0.815** | **0.863** | **0.915** | 0.961 | **0.962** | **0.903** |
| STAGATE | 0.818 | 0.936 | 0.852 | 0.902 | 0.962 | 0.894 |
| STAMP v6.1 | 0.772 | 0.859 | 0.913 | **0.975** | 0.950 | 0.894 |
| scGLUE | 0.566 | 0.729 | 0.637 | 0.666 | 0.732 | 0.666 |
| Scanpy | 0.503 | 0.652 | 0.439 | 0.690 | 0.760 | 0.609 |
| MultiVI | 0.300 | 0.571 | 0.542 | 0.430 | 0.670 | 0.503 |
| COSMOS | 0.062 | 0.444 | 0.403 | 0.700 | 0.675 | 0.457 |
| GraphST | 0.018 | 0.353 | 0.363 | 0.636 | 0.373 | 0.348 |
| MUSE | — | -0.021 | 0.080 | 0.039 | 0.008 | 0.026 |

### 4.3 STAMP v6.1 vs v7a 对比

| 指标 | D1 | D2 | D3 | D4 | D5 | **Mean** |
|------|-----|-----|-----|-----|-----|---------|
| v6.1 ARI | 0.772 | 0.859 | 0.913 | **0.975** | 0.950 | 0.894 |
| v7a ARI | **0.815** | **0.863** | **0.915** | 0.961 | **0.962** | **0.903** |
| Δ ARI | **+0.043** | +0.004 | +0.003 | -0.014 | +0.012 | **+0.010** |

### 4.4 关键可视化结果

所有可视化结果位于 `code/figures_v7a/` 目录下：

- **空间域识别图** (`stamp_spatial_domain_d{1-5}.png`)：左图为模型预测，右图为 Ground Truth，颜色统一映射
- **UMAP 降维图** (`stamp_umap_d{1-5}.png`)：左图为预测 cluster，右图为 Ground Truth 细胞类型
- **Benchmark 对比图**：v6.1 vs v7a 柱状图、全方法平均 ARI 排名图、单模态分解图

---

## 5. 分析讨论

### 5.1 方法优劣分析

**STAMPv7a 的优势**：
- 平均 ARI 0.903，已超越 STAGATE（0.894）和 v6.1 baseline，接近 SpatialGlue（0.936）
- 在最难数据集 D1 上提升最显著（+0.043），说明双图并行有效补充了空间图的信息盲区
- 实现简洁（~260 行核心代码），训练稳定（早停通常在 400-800 epoch）

**劣势与局限**：
- 与 SpatialGlue 仍有 0.03 的 ARI 差距，主要输在 D1（0.815 vs 0.882）
- D4 上 v7a 略低于 v6.1（-0.014），特征图在高信噪比数据上可能引入轻微过平滑
- RNA 单分支质量仍是瓶颈（平均 ARI 仅 0.35），融合结果在多数数据集上仍略低于 ATAC 单模态

### 5.2 多组学融合 vs 单组学

| 数据集 | z_r (RNA) | z_a (ATAC) | z_stamp (融合) | 融合增益 |
|--------|-----------|------------|----------------|---------|
| D1 | 0.349 | 0.827 | **0.815** | -0.012 vs ATAC |
| D2 | 0.216 | 0.893 | **0.863** | -0.030 vs ATAC |
| D3 | 0.414 | 0.919 | **0.915** | -0.004 vs ATAC |
| D4 | 0.353 | 0.962 | **0.961** | -0.001 vs ATAC |
| D5 | 0.433 | 0.960 | **0.962** | +0.002 vs ATAC |

ATAC 单模态仍是主导信号（平均 ARI=0.912）。融合在 D5 上首次超越 ATAC 单模态（0.962 vs 0.960），但在 D1/D2 上略低，说明 RNA 分支的噪声仍然对融合有轻微拖累。这提示：**当一种模态质量显著高于另一种时，融合策略需要更保守地引入低质量模态的信息**。

### 5.3 设计选择的有效性验证

| 设计选择 | 变体 | D1 ARI | 结论 |
|---------|------|--------|------|
| **双图并行 (v7a)** | 单图 (v6.1) | 0.772 → **0.815** | **有效，+0.043** |
| 非对称 CMA | 双向对称 CMA | 0.76 → 0.74 | 非对称设计有效保护 ATAC |
| 跨模态重建 | 仅自重建 (v9) | 0.24 | 跨模态重建是防止坍塌的必要条件 |
| Stop-gradient 对齐 | 直接 MSE 对齐 | 0.76 → 0.68 | Stop-gradient 防止对称梯度坍塌 |
| PCA 维度 | PCA 100 / PCA 300 | 0.75 / 0.37 | PCA 50 是最佳维度 |

---

# Part B: Vibe Coding 全流程记录

## 1. Deep Research 过程

### 1.1 调研策略

使用 Kimi 深度研究功能进行了三轮系统性调研：

**第一轮：宏观技术路线调研**
> "请帮我系统调研空间多组学（spatial multi-omics）数据中 graph-based 多模态融合算法的最新进展。我需要了解：1) 目前主流方法有哪些？2) 空间邻域图如何构建？3) 跨模态融合策略有哪些？4) 评估指标和 benchmark 数据集？"

Deep Research 返回了 SpatialGlue、SpaDDM、STAGATE、GraphST、MultiGATE 等方法的系统总结，并整理了空间图构建策略（kNN、Delaunay、radius）和融合阶段策略（早期/中期/晚期融合）。

**第二轮：关键技术细节深挖**
> "SpatialGlue 的双重注意力机制具体是如何工作的？within-modality attention 和 between-modality attention 的输入输出维度是什么？"

这轮回答帮我理解了 SpatialGlue 的分层注意力架构：每个模态内部先对空间图和特征图做 within-modality attention，然后两个模态之间做 between-modality attention。这直接启发了我设计"双图编码器"的思路。

**第三轮：设计方向探索**
> "如果我想设计一个简化但有效的空间多组学融合方法，哪些组件是必不可少的？哪些可以去掉？"

这轮回答指出：空间图约束、跨模态对齐、以及防止表示坍塌的约束（如重建或对比损失）是三个核心要素。注意力机制的"可解释性"虽然漂亮，但并非性能提升的关键。

### 1.2 调研中的误导与纠正

Deep Research 的一个误导性回答是："建议对每种模态使用独立的空间图构建策略，因为 RNA 和 ATAC 的空间分布特征不同"。实际上，在我们的模拟数据中，RNA 和 ATAC 共享完全相同的空间坐标，独立构建空间图毫无意义。这个误导在代码实现阶段被及时发现并纠正。

另一个误导是 Deep Research 过度强调了"对比学习"的效果，声称"InfoNCE 损失是跨模态对齐的首选方案"。但我们的实验表明，在模态质量极不均衡的场景中（RNA 噪声大、ATAC 质量好），简单的 stop-gradient MSE 对齐配合跨模态重建，比 InfoNCE 更稳定。

---

## 2. plan.md 的演化

### 2.1 最初版本（v1）

最初的 plan.md 是一份极其雄心勃勃的设计文档，包含：
- **多尺度空间图金字塔**（Micro/Meso/Macro 三层）
- **SparseGAT + Peak 选择门**（Gumbel-Softmax 选择 Top-K peaks）
- **空间感知跨模态对比损失**（邻居作为弱正样本）
- **自适应模态置信度**（基于局部重构误差的动态权重）
- **Hadamard 交互项**（捕捉非线性协同信号）

这个设计试图一次性解决所有问题，但复杂度远超 AI 的实现能力。

### 2.2 演化路径

| 版本 | 主要变化 | 原因 |
|------|---------|------|
| **v1** | 完整四阶段流水线（上述所有模块） | 设计过度复杂 |
| **v2** | 去掉多尺度图金字塔，改为单层双图 | 多尺度参数过多，训练不稳定 |
| **v3** | 去掉 SparseGAT 和 Peak 选择门 | ATAC 预处理用 LSI 已足够，无需复杂 Peak 选择 |
| **v4** | 去掉自适应模态置信度（AMC） | 静态拼接已能达到相似效果，AMC 增加不稳定性 |
| **v5** | 基础版：双图 + 非对称 CMA + 跨模态重建 | 第一个可运行的稳定版本 |
| **v6.1** | 稳定版：优化损失权重和训练流程 | ARI=0.894，成为可靠 baseline |
| **v7** | 引入 RNA 预训练 + 自适应融合门控 | 试图进一步提升，但失败 |
| **v7a** | 仅保留双图并行，去掉预训练和门控 | **最终成功版本，ARI=0.903** |

### 2.3 关键教训

plan.md 的演化揭示了一个重要规律：**在 Vibe Coding 中，AI 能可靠实现的复杂度有一个上限**。最初的设计文档虽然理论上更优雅，但包含太多需要精细调参的组件（Gumbel-Softmax、动态图更新、三层尺度注意力），AI 在实现时频繁出现维度不匹配、梯度消失、训练崩溃等问题。

最终的成功版本 v7a 是一个"做减法"的结果：保留了被验证有效的核心组件（双图、非对称 CMA、跨模态重建），去掉了所有"锦上添花"但增加复杂度的模块。

---

## 3. 关键 Prompt 展示与分析

### Prompt 1：初始设计指令（有效）

> "基于以下调研结果，设计一个空间多组学融合算法。核心要求：1) 使用 PyTorch Geometric 的 GATConv 构建图编码器；2) 处理 RNA+ATAC 两种模态；3) 每个模态同时接收空间邻域图和特征相似性图；4) 使用 cross-attention 实现模态融合；5) 输出 30 维潜在表示用于 Leiden 聚类。请写出完整的 PyTorch 模型类。"

**效果分析**：这个 prompt 非常具体（指定了库、模块类型、维度），AI 成功生成了初版模型代码。成功的关键是：给出了明确的输入输出规格和组件选择。

### Prompt 2：Debug 指令（有效）

> "训练 loss 在 50 epoch 后不再下降，稳定在 0.3 左右，ARI 只有 0.15。请分析可能原因。模型结构是：[粘贴模型代码]。训练循环是：[粘贴训练代码]。"

**效果分析**：提供了完整的现象描述（loss 停滞值、ARI 值）和代码上下文，AI 准确诊断出问题：缺少跨模态重建损失导致表示坍塌。这是整个项目最关键的修复之一。

### Prompt 3：消融实验指令（有效）

> "请在这个模型的基础上做以下消融实验：1) 去掉 CMA 层，直接拼接 z_r 和 z_a；2) 将非对称 CMA 改为对称双向；3) 去掉空间对比损失。每个变体跑 Dataset 1，记录 ARI。"

**效果分析**：AI 成功生成了三个变体并运行了实验。消融实验的自动化是 Vibe Coding 的巨大优势——人类只需要提出假设，AI 负责实现和验证。

### Prompt 4：可视化修复指令（无效 → 有效）

**第一次（无效）**：
> "空间图显示全灰色，请修复。"

AI 回复猜测是 "palette 参数问题"，修改了颜色映射但没有解决根本问题。

**第二次（有效）**：
> "空间图显示全灰色，图例显示 'NA'。我检查了代码，发现 `adata_out.obs['stamp_domain'] = ad_tmp.obs['leiden'].astype(str)` 这行，其中 ad_tmp 是新创建的 AnnData。请分析为什么这会导致全灰。"

AI 准确诊断出索引对齐问题（`adata_out` 保留原始索引，`ad_tmp` 使用默认 RangeIndex），并提供了修复方案。

**教训**：描述现象时提供代码上下文和观察到的细节（"图例显示 NA"），比单纯说"出错了"有效得多。

---

## 4. Debug 案例

### Case 1：RNA 预训练导致性能下降（v7 失败）

**现象**：在 v6.1 基础上引入"RNA 预训练"（先用 RNA 单模态训练 500 epoch，再加载权重进行跨模态训练），D1 ARI 从 0.772 降至 0.769。

**分析过程**：
1. 检查 z_r 单分支 ARI：从 0.52 降至 0.48，说明预训练学到的 RNA 表示不适合后续融合
2. 假设：预训练阶段缺乏跨模态对齐约束，RNA 编码器过拟合到 RNA-specific 信号
3. 验证：去掉预训练，改为端到端训练 → D1 ARI 恢复至 0.815

**根因**：预训练目标（RNA 自重建）与最终目标（跨模态融合聚类）不一致。预训练学到的表示空间与融合任务的需求空间存在"目标偏移"。

**最终方案**：彻底去掉预训练，所有参数端到端训练。

### Case 2：自适应融合门控饱和（v7 失败）

**现象**：引入自适应门控（每个 spot 学习 RNA/ATAC 的动态融合权重），但训练后门控值恒定为 ~0.97，失去动态调节能力。

**分析过程**：
1. 检查门控初始化：bias=0.4，sigmoid 后初始值 ~0.60
2. 观察训练过程：前 10 个 epoch 门控就飙升至 0.97，之后不再变化
3. 假设：门控初始值太高，加上 ATAC 信号强，模型迅速学到"总是信任 ATAC"
4. 验证：将 bias 从 0.4 改为 -2.0 → 初始值 ~0.12，但门控仍然收敛到极端值

**根因**：在模态质量极不均衡的场景中，自适应门控容易被主导模态"劫持"。ATAC 的高质量信号使得模型有强烈的动机将权重推向极端。

**最终方案**：彻底去掉自适应门控，改为静态可学习标量 `graph_alpha`（仅控制双图融合比例）。

### Case 3：可视化颜色全灰（索引对齐 bug）

**现象**：Spatial plot 左图（预测）全灰，图例显示 "NA"；右图（Ground Truth）正常。

**分析过程**：
1. 检查 `stamp_domain` 列的值：大部分为 NaN
2. 追溯赋值代码：`adata_out.obs['stamp_domain'] = ad_tmp.obs['leiden'].astype(str)`
3. 发现 `ad_tmp = sc.AnnData(emb_reduced)` 是新建对象，索引为 `RangeIndex(0, n)`
4. `adata_out` 保留原始 h5ad 索引。Pandas 按索引对齐赋值时，不匹配的位置变为 NaN

**修复**：在 grid search 循环中保存 `best_labels = pred.copy()`，然后直接赋值：`adata_out.obs['stamp_domain'] = best_labels.astype(str)`

---

## 5. AI 错误模式总结

### 5.1 AI 表现好的方面

- **标准组件实现**：GATConv、BatchNorm、残差连接等标准 PyG 模块的实现几乎不会出错
- **数据预处理流程**：scanpy 的标准预处理（normalize、log1p、HVG、PCA、LSI）实现准确
- **评估指标计算**：ARI、NMI、UMAP、spatial plot 的代码生成正确
- **消融实验变体**：给定明确的修改指令，AI 能快速生成变体代码

### 5.2 AI 经常出错的方面

- **维度不匹配**：在 cross-attention 中频繁混淆 (batch, seq, dim) 和 (nodes, features) 的维度顺序
- **图结构处理**：对 `edge_index` 的 (2, n_edges) 格式理解不稳定，有时写成 (n_edges, 2)
- **训练逻辑 bug**：如忘记 `.train()`/`.eval()` 切换、optimizer.zero_grad() 位置错误
- **过度工程化**：当 prompt 不够具体时，AI 倾向于添加不必要的复杂组件（如额外的 BatchNorm、多余的激活函数）

### 5.3 发现的规律

- **越具体的 prompt，错误率越低**：包含具体维度、参数值、库名的指令，代码正确率显著高于模糊描述
- **AI 擅长"做什么"，不擅长"为什么"**：AI 能准确实现给定的设计，但难以判断该设计是否合理。例如 AI 不会主动指出"RNA 预训练可能与融合目标冲突"
- **Debug 时需要"喂数据"**：给 AI 看具体的 loss 曲线、参数值、中间输出 shape，比抽象描述有效 10 倍

---

## 6. 设计妥协记录

| 原始设计 | 妥协后方案 | 妥协原因 | 对结果的影响 |
|---------|-----------|---------|-------------|
| 多尺度空间图金字塔（3层） | 单层双图（空间+特征） | 多尺度参数过多，训练不稳定 | 性能损失极小，D1 仍提升 +0.043 |
| SparseGAT + Gumbel Peak 选择 | 标准 GAT + LSI 预处理 | Gumbel-Softmax 实现复杂，梯度不稳定 | ATAC 质量未下降，LSI 已足够 |
| 自适应模态置信度（AMC） | 静态拼接 concat([z_r, z_a]) | AMC 被 ATAC 主导模态"劫持" | 性能反而提升，去除动态门控更稳定 |
| 空间感知对比学习（邻居作弱正样本） | 简单 InfoNCE 在同一点对上 | 弱正样本实现复杂，收益不明确 | 当前 InfoNCE 已足够 |
| RNA 预训练（500 epoch） | 端到端训练 | 预训练目标与融合目标不一致 | 去除后 D1 ARI 从 0.769 提升至 0.815 |
| Hadamard 交互项 | 去除 | 增加复杂度但无明确收益 | 无负面影响 |

**总体评估**：所有妥协都是"做减法"，最终版本比最初设计简单得多，但性能反而更好。这说明在空间多组学融合任务中，**简洁性和训练稳定性可能比架构复杂度更重要**。

---

# Part C: 反思

## 1. 最有挑战的环节

**从调研到设计的转化是最有挑战的环节**，原因有三：

第一，文献中的方法描述往往是"理想化"的。例如 SpatialGlue 的论文只展示了最终架构，没有透露训练中遇到的稳定性问题。当我按照论文描述实现 dual-attention 时，发现训练经常崩溃，需要大量试错才能找到稳定的配置。

第二，**设计选择之间存在复杂的耦合关系**。例如"非对称 CMA"和"跨模态重建"不是独立的设计——只有当 CMA 是单向的（RNA←ATAC），跨模态重建才能保持循环一致性。如果改成双向 CMA，重建损失的设计也需要重新推导。

第三，**评估指标与训练目标之间的鸿沟**。训练时优化的是重建损失 + 对齐损失，但最终评估的是聚类 ARI。这两个目标并不完全对齐——有时 loss 下降了但 ARI 也下降，反之亦然。如何设计训练目标以"代理"最终评估指标，是一个开放问题。

## 2. 如果允许手写代码，我会在哪些地方自己写？

如果有手写代码的自由，我会选择以下三个地方自己写：

**（1）核心模型 forward 函数**

AI 在实现 attention 机制时经常搞混维度，导致需要反复调试。手写 forward 函数可以确保维度正确，同时对计算图有完全的控制。

**（2）训练循环和早停逻辑**

AI 生成的训练循环通常是"模板化"的，不会针对具体任务做优化。例如我们的早停是基于总 loss 的，但实际上应该基于验证集 ARI（虽然我们没用验证集）。手写训练循环可以更灵活地实现这种定制逻辑。

**（3）后处理的 grid search**

后处理参数（smooth_iter、pca_dim、resolution）的搜索策略对最终结果影响巨大。AI 实现的 grid search 是标准的嵌套循环，但手动调整可以发现更高效的搜索策略（如 coarse-to-fine 搜索）。

## 3. 下一步改进方向

**短期（1-2 周）**：
- **自适应特征图邻居数**：当前固定 k=15，可根据局部密度自适应调整（如 D4 可能需要更小的 k）
- **RNA 分支增强**：引入 STAGATE 式的深度预训练（带空间约束），但预训练目标必须与融合目标一致

**中期（1 个月）**：
- **保守的自适应门控**：在 v7a 基础上叠加一个初始化 confidence≈0.3 的门控，避免被主导模态劫持
- **理论上限探索**：实验表明将 STAGATE RNA 嵌入（ARI~0.82）与 STAMP ATAC 嵌入拼接可达 ARI=0.886，说明融合天花板很高

**长期**：
- 在真实数据（Mouse Brain P22）上验证方法泛化性
- 探索 RNA + Protein（而非 RNA + ATAC）模态组合下的性能

## 4. Vibe Coding 在计算生物学研究中的适用边界

**适用场景**：
- **快速原型验证**：几天内实现一个可运行的 baseline，验证设计思路的可行性
- **消融实验自动化**：给定假设，AI 快速生成变体并运行对比
- **标准组件集成**：数据预处理、评估指标、可视化等"工程性"代码
- **文献复现**：有明确算法描述和参数设置时，复现效率极高

**不适用场景**：
- **需要深度数学推导的模块**：如证明某个损失函数的收敛性、推导最优解的闭式形式
- **需要领域知识做判断的设计选择**：如"RNA 预训练是否与融合目标冲突"——这需要生物学直觉，AI 无法替代
- **调试复杂的训练动态问题**：如"为什么门控会饱和到 0.97"——AI 可以提出假设，但验证假设需要人类的系统思维
- **超参数调优的"最后一公里"**：AI 可以跑 grid search，但判断"哪个参数组合真正最优"需要经验

**总结**：Vibe Coding 是一个强大的"加速器"，但不是"替代品"。它最适合处理"已知问题的工程实现"，而"未知问题的科学发现"仍然需要人类的洞察力和判断力。

---

# 附录：参考文献

1. Long Y, et al. Deciphering spatial domains from spatial multi-omics with SpatialGlue. *Nature Methods*. 2024;21:1658-1667.
2. Wang et al. (2026). Dissecting spatial patterning and signaling with directional diffusion in spatial multi-omics. *PNAS* 123(10).
3. Dong & Zhang (2022). Deciphering spatial domains from spatially resolved transcriptomics with an adaptive graph attention autoencoder. *Nature Communications* 13, 1739.
4. Miao J, et al. MultiGATE: integrative analysis and regulatory inference in spatial multi-omics data via graph representation learning. *Nature Communications*. 2025;16:9403.
5. Yan X, et al. Mosaic integration of spatial multi-omics with SpaMosaic. *Nature Genetics*. 2026.
6. Long Y, et al. Spatially informed clustering, integration, and deconvolution of spatial transcriptomics with GraphST. *Nature Communications*. 2023;14:1155.

---

*报告生成日期：2026年5月18日*
*代码仓库：/data/lvyongji/Assignment5/code/*
*可视化结果：/data/lvyongji/Assignment5/code/figures_v7a/*
