# 空间多组学图神经网络多模态融合算法研究笔记

## 1. 关键方法核心架构一句话总结

| 方法              | 核心架构一句话总结                                                               |
| --------------- | ----------------------------------------------------------------------- |
| **MultiGATE**   | 采用双层图注意力网络（模态内+模态间）的自编码器框架，通过独立编码器处理维度差异，实现拓扑保持的联合嵌入与调控关系推断。            |
| **SpatialGlue** | 基于GNN的双重注意力聚合架构，显式并行处理空间邻近图与特征相似性图，通过分层注意力（模态内→模态间）实现自适应融合。             |
| **SpaMosaic**   | 针对马赛克数据设计的对比学习框架，结合轻量GCN编码器，通过"空间边+表达边"异质图结构实现跨切片、跨模态的统一对齐。             |
| **CANDIES**     | 两阶段生成-判别架构（扩散Transformer去噪 → GCN跨模态对齐），利用条件扩散模型处理质量不均衡模态，再通过对比学习实现精细对齐。 |
| **SMODEL**      | 基于双图正则化（空间图+组学相似图）的集成学习框架，采用"先分离学习、后集成共识"策略，通过独立学习器层避免早期融合噪声。           |
| **scMODAL**     | 多编码器共享潜在空间配合Feature Links机制，显式编码生物学先验（如peak-基因调控关系），实现生物学约束的对齐。         |
| **stClinic**    | 动态空间图架构，通过切片内物理边与切片间表达边（MNN匹配）构建异质图，支持跨切片的三维生态位追踪与连续性分析。                |

## 2. 空间图构建常用策略

### 2.1 基础构建方法

| 策略 | 机制描述 | 关键参数 | 适用场景 |
|------|---------|---------|---------|
| **k-近邻 (kNN)** | 为每个节点保留k个最近空间邻居，自适应局部密度（密集区覆盖小范围，稀疏区覆盖大范围） | k=10~30（Visium），50~100（高分辨率） | 通用场景，平衡分辨率与上下文 |
| **距离阈值 (Radius-based)** | 固定搜索半径r，自适应确定邻居数量，具有物理可解释性（如细胞通讯有效距离） | r=150-200μm（Visium），50-100μm（单细胞） | 已知生物学作用尺度的场景 |
| **Delaunay三角剖分** | 基于计算几何的三角化覆盖，边自然定义邻域关系，避免kNN的长距离连接 | 无 | 保持精确拓扑结构，防止长距离伪边 |
| **网格拓扑 (Grid-based)** | **Visium平台**：六边形spot排列，使用`n_rings`参数（n_rings=1为6邻居，n_rings=2为18邻居）&lt;br&gt;**Stereo-seq**：四邻居或八邻居连接 | n_rings, 网格类型 | 特定平台的标准化处理 |

### 2.2 多切片/跨模态异质图构建

**异质边设计**：
- **空间边 (Spatial Edges)**：严格限定于同一切片内部，基于物理距离（kNN/radius）构建，确保空间局部性可靠性
- **表达边 (Expression Edges)**：跨越不同切片，基于批次校正后的低维表示通过**互最近邻 (MNN)** 算法识别功能相似节点对，权重通常低于空间边

**动态图更新 (stClinic)**：
- 切片边界区域：提升表达边权重以补偿空间连续性中断
- 切片内部：空间边主导以强化局部结构
- 切片间边构建：互惠验证（i在B中的k近邻且j在A中的k近邻时才建立连接）

### 2.3 实践工具与参数建议

| 工具 | 功能 | 推荐配置 |
|------|------|---------|
| **Squidpy** | Python空间组学分析库 | `sq.gr.spatial_neighbors()`，支持kNN/半径/Delaunay |
| **imcRtools** | R语言成像质谱分析 | `buildSpatialGraph()`，支持expansion/delaunay/knn |
| **Giotto** | 综合空间分析平台 | 端到端分析，交互式可视化 |

**边界处理策略**：边缘检测与掩码、镜像填充扩展边界、基于组织形态的自适应k调整（避免边缘spot邻居不足导致的表示偏差）

## 3. 跨模态融合设计选择及 Trade-off

### 3.1 融合阶段策略比较

| 阶段 | 策略描述 | 代表方法 | 优势 | Trade-off/局限 |
|------|---------|---------|------|---------------|
| **早期融合** | 原始特征拼接后统一建模 | 简单拼接+PCA/聚类 | 实现简单，无需复杂架构 | **噪声放大**：模态间冲突未处理，维度灾难，技术噪声相互传播 |
| **中期融合** | 隐藏层交互，学习联合表示 | MultiGATE, SpatialGlue, CANDIES, SpaMosaic | **平衡灵活性与复杂性**，注意力/对比机制实现自适应融合，保留模态特异性同时实现交互 | **架构复杂**，超参数调优需求高，计算开销较大 |
| **晚期融合** | 独立建模后决策层集成 | SMODEL（集成共识机制） | **模态独立性高**，易于模块化扩展，对模态缺失鲁棒，避免早期噪声传播 | **协同信息损失**：可能丢失模态间细微关联，集成权重设计挑战，难以捕获非线性交互 |

### 3.2 具体融合机制 Trade-off

#### A. 注意力引导融合 (Attention-Guided)
- **机制**：节点级自注意力（特征维度权重）→ 邻居级空间注意力（动态邻居贡献）→ 跨模态注意力（动态模态重要性调节）
- **优势**：可解释性强（注意力热图可视化关键邻居/模态），动态适应局部细胞状态，多头机制（8-12头）捕获多尺度模式
- **Trade-off**：计算复杂度随头数增加，注意力权重可能过于集中于主导模态，需配合模态平衡机制

#### B. 对比学习对齐 (Contrastive Learning)
- **机制**：使用InfoNCE损失最大化正样本对（同一空间单元的跨模态嵌入）相似度，最小化负样本对相似度（温度参数τ=0.07控制聚集度）
- **优势**：利用自然共现对（同一spot的多模态观测）作为"免费"监督信号，实现高质量的零样本迁移与跨模态检索（Recall@10可达0.94）
- **Trade-off**：负样本构造策略敏感（简单随机负样本可能包含假阴性），对批次大小敏感（需要足够大的batch包含有效负样本），低温可能导致过度聚集

#### C. 子空间分解 (Subspace Decomposition)
- **机制**：显式分离共享子空间（跨模态一致的细胞状态）与私有子空间（模态特异性变异，如转录组剪接异构体、蛋白组翻译后修饰）
- **实现**：多任务学习、对抗学习（梯度反转层）、变分推断（分别为共享/私有信息建模潜在变量）
- **Trade-off**：避免"过度对齐"导致的特异性信号丢失，但**分解质量依赖先验假设**，共享与私有的边界模糊时可能失效，增加模型复杂度

#### D. 生成式融合 (Generative Fusion)
- **机制**：跨模态解码器（从模态A嵌入预测模态B特征，循环一致性约束双向重建）、条件扩散模型（CANDIES使用DiT以高质量模态引导低质量模态去噪）
- **优势**：处理质量不均衡模态（如高噪声表观组+高质量转录组），支持插值生成探索生物学状态，潜在空间更平滑
- **Trade-off**：两阶段设计（CANDIES：去噪→对齐）**显著增加计算开销**，生成训练不稳定，对超参数敏感

### 3.3 关键设计决策清单

**维度差异处理**：
- **独立编码器+共享空间**：主流策略，潜在维度通常15-30维（需交叉验证，过低丢失信号，过高对齐困难）
- **模态平衡采样**：梯度归一化或动态权重调整，避免单模态梯度主导优化过程
- **加权损失函数**：WMSE = Σ_i α_i × (1/|X_i|) Σ M(x) × (x - x̂)²（维度归一化误差比较）

**空间约束强度调节**：
- **过度平滑风险**：过高空间约束导致真实生物学边界模糊（肿瘤-正常交界、发育梯度区域）
- **消融实验基准**：去除空间图约束后ARI通常下降0.05-0.10，可作为约束强度参考

**鲁棒性机制**：
- **马赛克场景**：SpaMosaic的伪配对机制利用表达边构建跨模态关系，适用于临床样本的缺失模态插补
- **质量不均衡**：CANDIES的去噪-整合两阶段设计显式处理低信噪比模态
- **批次效应**：MNN匹配、对抗训练、显式批次校正模块（如SpatialMETA的CVAE架构）

## 4. 评估指标速查

| 类别        | 指标        | 最优值     | 应用场景                      |
| --------- | --------- | ------- | ------------------------- |
| **聚类质量**  | ARI       | 1.0     | 有标签评估（SpatialGlue可达0.973） |
|           | NMI       | 1.0     | 信息论视角，对类别数差异稳健            |
| **空间一致性** | Moran's I | &gt;0.3 | 空间自相关，衡量空间域连续性            |
| **对齐质量**  | Recall@K  | 高       | 跨模态检索，验证语义一致性             |
| **批次去除**  | kBET      | 1.0     | 批次效应去除程度                  |