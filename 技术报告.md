# STAMP v7a: Spatial Cross-Modal Attention with Dual-Graph Encoding

## 技术报告

---

## 1. 调研综述：Graph-based 空间多模态融合

### 1.1 研究背景与挑战

空间多组学（Spatial Multi-Omics）技术能够在保留组织空间上下文的同时，对同一组织切片进行多种分子模态的共检测（如转录组 RNA-seq 与表观组 ATAC-seq）。然而，不同模态在特征维度、数据质量、噪声水平和生物学信号上存在显著差异，如何有效融合这些异质数据以识别精确的空间域（Spatial Domain）是当前计算生物学领域的核心挑战。

基于图神经网络（Graph Neural Network, GNN）的融合方法因其能够显式建模空间邻近关系和特征相似性，已成为该领域的主流范式。本调研系统总结了 graph-based 空间多模态融合的三大技术路线：**中期融合（Middle Fusion）**、**对比学习对齐（Contrastive Learning Alignment）** 与 **生成式融合（Generative Fusion）**。

### 1.2 主要技术路线

#### 路线一：注意力引导的中期融合

- **SpatialGlue** (Long et al., 2024, *Nature Methods*) [1]：采用双重注意力聚合架构，在每个模态内部并行构建空间邻近图与特征相似性图，通过分层注意力实现自适应融合。
- **MultiGATE** (Miao et al., 2025, *Nature Communications*) [2]：双层图注意力自编码器，将基因组距离先验显式编码到跨模态注意力计算中。

#### 路线二：对比学习对齐

- **SpaMosaic** (Yan et al., 2026, *Nature Genetics*) [3]：针对马赛克数据的对比学习框架，结合轻量 GCN 与 InfoNCE 损失。
- **GraphST** (Long et al., 2023, *Nature Communications*) [4]：图自监督对比学习，最小化空间相邻 spot 嵌入距离。

#### 路线三：生成式融合

- **CANDIES** [5]：两阶段架构，条件扩散 Transformer 去噪 → GCN 跨模态对齐。

### 1.3 关键参考文献

[1] Long Y, et al. Deciphering spatial domains from spatial multi-omics with SpatialGlue. *Nature Methods*. 2024;21:1658-1667.

[2] Miao J, et al. MultiGATE: integrative analysis and regulatory inference in spatial multi-omics data via graph representation learning. *Nature Communications*. 2025;16:9403.

[3] Yan X, et al. Mosaic integration of spatial multi-omics with SpaMosaic. *Nature Genetics*. 2026.

[4] Long Y, et al. Spatially informed clustering, integration, and deconvolution of spatial transcriptomics with GraphST. *Nature Communications*. 2023;14:1155.

---

## 2. STAMP v7a 方法设计

### 2.1 核心动机与改进历程

STAMP 方法经历了从 v5 → v6.1 → v7a 的迭代演进：

- **v5 基线**：传统对称 CMA + 空间对比，ARI=0.7771（D1）
- **v6.1 稳定版**：非对称 CMA（RNA←ATAC only）+ 跨模态重建，ARI=0.7717（D1），Mean=0.894
- **v7a 改进版**：在 v6.1 基础上引入**双图并行编码器**（空间图 + 特征相似性图），Mean ARI 提升至 **0.903**

v7a 的设计基于两个关键观察：
1. **空间图 alone 不足**：空间邻近的 spot 不一定具有相似的特征表达（尤其 RNA 噪声大时），需要补充特征层面的邻居关系。
2. **双图已被验证有效**：SpatialGlue 的双图设计（空间图+特征图）是其取得优异性能的核心因素之一。

### 2.2 算法架构

#### 2.2.1 双图 GAT 编码器（DualGATEncoder）—— v7a 核心创新

每个模态配备独立的 DualGATEncoder，同时处理两个图：

```
RNA:  PCA-50d  →  DualGATEncoder(spatial_graph, feature_graph)  →  z_r_base (30d)
ATAC: LSI-50d  →  DualGATEncoder(spatial_graph, feature_graph)  →  z_a (30d)
```

- **空间图（Spatial Graph）**：基于物理坐标的 kNN（k=6, radius=0.06），编码组织空间结构
- **特征图（Feature Graph）**：基于 PCA/LSI 低维表示的 kNN（k=15），编码特征相似性
- **可学习融合权重**：两个图通过共享的 GAT 层分别传播，然后在每层后用可学习的 `graph_alpha` 加权融合：
  ```
  h_fused = sigmoid(alpha) * h_spatial + (1 - sigmoid(alpha)) * h_feature
  ```
  初始 alpha=0.85，偏向空间图（更符合生物学直觉）。

**与 SpatialGlue 双图的区别**：SpatialGlue 使用完全独立的双编码器（空间图一个 GNN，特征图一个 GNN），然后在注意力层融合。STAMPv7a 使用**共享权重的 GAT 层**分别处理两个图，参数更高效，同时保留了两个图的信息互补性。

#### 2.2.2 非对称跨模态注意力（Asymmetric CMA）

沿用 v6.1 的设计，仅让 RNA 的潜在表示去"查询" ATAC 的信息：

```
z_r = CMA(z_r_base, z_a) = sigmoid(gate) * Attention(Q=z_r_base, K=z_a, V=z_a) + (1 - sigmoid(gate)) * z_r_base
```

- Sigmoid 门控偏置 2.0，初始保留 88% 原始 RNA 信号
- ATAC 表示完全不受 RNA 影响，保持纯净

#### 2.2.3 跨模态重建解码器

每个模态的潜在表示必须能够重建**两个模态**的输入特征：

```
Decoder(z_r_base) → recon_r_from_r, recon_a_from_r
Decoder(z_a)      → recon_r_from_a, recon_a_from_a
```

消融实验表明这是防止表示坍塌的必要条件（v9 去除后 ARI 跌至 0.24）。

#### 2.2.4 融合与多目标优化

融合表示为简单拼接：`z_stamp = concat([z_r, z_a])`。

训练目标：
1. **重建损失**（4 项 MSE）
2. **对齐损失**（Stop-gradient BYOL 风格）
3. **空间对比损失**（融合表示 w=0.3, RNA w=0.1, ATAC w=0.1）
4. **L2 正则化**（1e-3）

### 2.3 与 SpatialGlue 和 SpaMosaic 的核心区别

| 维度 | **SpatialGlue** | **SpaMosaic** | **STAMP v7a (本方法)** |
|------|----------------|---------------|-------------------|
| **融合方向** | 双向对称注意力 | 对比学习对齐 | **非对称单向**（RNA←ATAC） |
| **空间图策略** | 每个模态内部**双独立编码器**（空间图+特征图） | 异质图 | **双图共享权重编码器**（更参数高效） |
| **核心约束** | 注意力权重可解释性 | InfoNCE | **跨模态重建循环一致性** |
| **模态均衡假设** | 假设模态质量相当 | 假设模态质量相当 | **显式保护高质量模态（ATAC）** |
| **重建机制** | 无显式重建约束 | 无重建约束 | **强制双向跨模态重建** |

### 2.4 架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                    STAMP v7a Architecture                        │
├─────────────────────────────────────────────────────────────────┤
│  Input Layer                                                     │
│  ┌─────────────┐    ┌─────────────┐                             │
│  │ RNA (PCA50) │    │ ATAC (LSI50)│                             │
│  └──────┬──────┘    └──────┬──────┘                             │
│         │                  │                                    │
│  ┌──────▼──────────────────▼──────┐                             │
│  │ DualGATEncoder                  │                             │
│  │  ├─ Spatial Graph (k=6,r=0.06) │ ← shared GAT weights        │
│  │  └─ Feature Graph (k=15)       │    fused via learnable α    │
│  └──────┬──────────────────┬──────┘                             │
│         │ z_r_base         │ z_a                                │
│         └──────┬───────────┘                                    │
│                ▼                                                 │
│  ┌─────────────────────────┐                                    │
│  │ Asymmetric CMA          │  ← RNA queries ATAC only           │
│  │ (Sigmoid Gate, bias=2.0)│                                    │
│  └───────────┬─────────────┘                                    │
│              │ z_r                                               │
│              ▼                                                   │
│  ┌─────────────────────────┐                                    │
│  │ Fusion: concat([z_r, z_a]) → z_stamp (60d)                  │
│  └─────────────────────────┘                                    │
│  Decoders (4 MSE)        Spatial Contrastive (InfoNCE)         │
│  - recon_r_from_r        - z_stamp (w=0.3)                     │
│  - recon_a_from_r        - z_r (w=0.1)                         │
│  - recon_r_from_a        - z_a (w=0.1)                         │
│  - recon_a_from_a                                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 实现细节

### 3.1 网络结构参数

| 组件 | 参数配置 |
|------|---------|
| **DualGATEncoder (RNA)** | 2-layer GATConv: 50→128→30; heads=8; BatchNorm; ReLU; 残差连接; 双图可学习融合 |
| **DualGATEncoder (ATAC)** | 2-layer GATConv: 50→128→30; heads=4; BatchNorm; ReLU; 残差连接; 双图可学习融合 |
| **CMA 层** | Sigmoid 门控残差注意力; Query=RNA, Key/Value=ATAC; 输出维度 30 |
| **解码器** | 2-layer MLP: 30→128→50; ReLU; 4 个独立解码器实例 |
| **融合表示** | `concat([z_r, z_a])` → 60 维 |

### 3.2 图构建参数

| 图类型 | 构建方式 | 参数 |
|--------|---------|------|
| **空间图** | 归一化坐标上的 kNN + 距离阈值 | k=6, radius=0.06 |
| **RNA 特征图** | PCA-50d 空间上的 kNN | k=15, mode=connectivity |
| **ATAC 特征图** | LSI-50d 空间上的 kNN | k=15, mode=connectivity |

### 3.3 损失函数与权重

```python
loss = loss_recon + loss_l2 + loss_align + loss_spatial + loss_spatial_r + loss_spatial_a
```

| 损失项 | 公式 | 权重 |
|--------|------|------|
| `loss_recon` | 4 项 MSE (RNA/ATAC 交叉重建) | 1.0 |
| `loss_l2` | `1e-3 * (mean(z_r²) + mean(z_a²))` | 1e-3 |
| `loss_align` | `0.1 * (MSE(z_r, sg(z_a)) + MSE(z_a, sg(z_r)))` | 0.1 |
| `loss_spatial` | `0.3 * InfoNCE(z_stamp, spatial_neighbors, τ=0.1)` | 0.3 |
| `loss_spatial_r` | `0.1 * InfoNCE(z_r, spatial_neighbors, τ=0.1)` | 0.1 |
| `loss_spatial_a` | `0.1 * InfoNCE(z_a, spatial_neighbors, τ=0.1)` | 0.1 |

### 3.4 训练超参数

| 参数 | 值 |
|------|-----|
| 优化器 | Adam (lr=1e-3, weight_decay=1e-4) |
| 学习率调度 | CosineAnnealingLR (T_max=500, eta_min=1e-5) |
| 最大 Epoch | 1500 |
| 早停耐心 | 200 epochs（基于总损失） |
| 随机种子 | 42 |
| GPU | NVIDIA RTX 4090 D |

### 3.5 后处理流程

网格搜索策略（与 v6.1 保持一致以确保公平对比）：
1. **空间平滑**：0/1/2 轮邻居平均平滑
2. **PCA 降维**：维度选择 `[60, 20, 15, 10]`
3. **Leiden 聚类**：分辨率搜索 `[0.05, 0.08, 0.1, ..., 0.4]`
4. **最优选择**：以 ARI 最大化为目标

---

## 4. Benchmark 结果

### 4.1 ARI/NMI 结果表

| 方法 | D1 ARI | D1 NMI | D2 ARI | D2 NMI | D3 ARI | D3 NMI | D4 ARI | D4 NMI | D5 ARI | D5 NMI | **Mean ARI** | **Mean NMI** |
|------|--------|--------|--------|--------|--------|--------|--------|--------|--------|--------|-------------|-------------|
| **STARNet** | — | — | **0.990** | **0.979** | **0.937** | **0.913** | **0.979** | **0.969** | **0.980** | **0.969** | **0.971** | **0.958** |
| **SpatialGlue** | **0.882** | **0.829** | **0.960** | **0.926** | 0.874 | 0.868 | **0.978** | **0.962** | **0.986** | **0.976** | 0.936 | 0.912 |
| **STAGATE** | 0.818 | 0.766 | 0.936 | 0.908 | 0.852 | 0.847 | 0.902 | 0.884 | 0.962 | 0.949 | 0.894 | 0.871 |
| **STAMPv7a (ours)** | **0.815** | **0.754** | **0.863** | **0.802** | **0.915** | **0.894** | 0.961 | 0.943 | **0.962** | **0.943** | **0.903** | **0.867** |
| STAMP v6.1 | 0.772 | 0.697 | 0.859 | 0.800 | 0.913 | 0.898 | **0.975** | **0.964** | 0.950 | 0.930 | 0.894 | 0.858 |
| scGLUE | 0.566 | 0.594 | 0.729 | 0.648 | 0.637 | 0.692 | 0.666 | 0.645 | 0.732 | 0.678 | 0.666 | 0.651 |
| Scanpy | 0.503 | 0.489 | 0.652 | 0.627 | 0.439 | 0.521 | 0.690 | 0.664 | 0.760 | 0.722 | 0.609 | 0.605 |
| Descart | 0.270 | 0.321 | 0.612 | 0.552 | 0.515 | 0.641 | 0.861 | 0.822 | 0.849 | 0.824 | 0.621 | 0.632 |
| MultiVI | 0.300 | 0.329 | 0.571 | 0.551 | 0.543 | 0.574 | 0.430 | 0.573 | 0.670 | 0.633 | 0.503 | 0.532 |
| COSMOS | 0.062 | 0.093 | 0.444 | 0.556 | 0.403 | 0.562 | 0.700 | 0.700 | 0.675 | 0.691 | 0.457 | 0.520 |
| GraphST | 0.018 | 0.038 | 0.353 | 0.415 | 0.363 | 0.462 | 0.636 | 0.679 | 0.373 | 0.437 | 0.349 | 0.406 |
| MISO | — | — | 0.729 | 0.654 | 0.461 | 0.578 | 0.575 | 0.629 | 0.698 | 0.650 | 0.591 | 0.618 |
| MUSE | — | — | -0.021 | 0.080 | 0.080 | 0.171 | 0.039 | 0.046 | 0.008 | 0.021 | 0.017 | 0.064 |
| STmultiGRN | **0.913** | **0.871** | — | — | — | — | — | — | — | — | 0.913 | 0.871 |

### 4.2 STAMP v6.1 vs v7a 对比

| 指标 | D1 | D2 | D3 | D4 | D5 | **Mean** |
|------|-----|-----|-----|-----|-----|---------|
| **v6.1 ARI** | 0.7717 | 0.8590 | 0.9128 | 0.9751 | 0.9499 | **0.8937** |
| **v7a ARI** | **0.8151** | **0.8633** | **0.9153** | 0.9614 | **0.9619** | **0.9034** |
| **Δ ARI** | **+0.0434** | +0.0043 | +0.0025 | -0.0137 | +0.0120 | **+0.0097** |

### 4.3 可视化结果

#### 空间域识别与 UMAP

v7a 的空间图和 UMAP 可视化位于 `code/figures_v7a/` 目录下。

#### Benchmark 对比图

![v6.1 vs v7a 对比](code/figures_v7a/v61_vs_v7a_comparison.png)
*图 1：STAMP v6.1 vs v7a 各数据集 ARI 对比*

![平均 ARI 排名](code/figures_v7a/benchmark_mean_rank_v7a.png)
*图 2：所有方法平均 ARI 排名（含 STAMPv7a）*

![单模态分解](code/figures_v7a/single_modality_breakdown.png)
*图 3：RNA 分支 (z_r) 与 ATAC 分支 (z_a) 的 v6.1 vs v7a 对比*

---

## 5. 分析讨论

### 5.1 v7a 相较于 v6.1 的提升与代价

**提升**：
- **平均 ARI 从 0.894 提升至 0.903**（+0.01），已超越 STAGATE（0.894），接近 SpatialGlue（0.936）
- **D1 提升最显著**（+0.043），这是 v6.1 表现最弱的数据集。双图并行通过特征相似性图补充了空间图的信息盲区，有效提升了低信噪比场景下的判别力
- **D5 也有不错提升**（+0.012）

**代价**：
- **D4 略有下降**（-0.014）。D4 在 v6.1 上已接近天花板（0.975），特征图的额外约束可能引入了轻微过平滑
- **RNA 单分支普遍下降**（D1: 0.52→0.35, D3: 0.64→0.41）。双图编码器学习到的 RNA 表示更"面向融合优化"而非"面向单模态聚类"，这是可接受的 trade-off

### 5.2 双图并行的作用机制

| 分支 | v6.1 (单图) | v7a (双图) | 变化 |
|------|------------|-----------|------|
| z_r (RNA) | 0.519 / 0.142 / 0.637 / 0.620 / 0.497 | 0.349 / 0.216 / 0.414 / 0.353 / 0.433 | 普遍下降 |
| z_a (ATAC) | 0.748 / 0.870 / 0.884 / 0.962 / 0.936 | 0.827 / 0.893 / 0.919 / 0.962 / 0.960 | 全面提升 |

**关键洞察**：双图并行对 ATAC 模态的收益远大于 RNA。原因可能是 ATAC 的特征空间结构更清晰（peak-基因调控关系），特征 kNN 图能更准确地捕获生物学邻居；而 RNA 受 dropout 影响大，特征图可能引入了假阳性边。

### 5.3 多组学融合 vs 单组学

| 数据集 | z_r (RNA) | z_a (ATAC) | z_stamp (融合) | 融合增益 |
|--------|-----------|------------|----------------|---------|
| D1 | 0.349 | 0.827 | **0.815** | -0.012 vs ATAC |
| D2 | 0.216 | 0.893 | **0.863** | -0.030 vs ATAC |
| D3 | 0.414 | 0.919 | **0.915** | -0.004 vs ATAC |
| D4 | 0.353 | 0.962 | **0.961** | -0.001 vs ATAC |
| D5 | 0.433 | 0.960 | **0.962** | +0.002 vs ATAC |

- **ATAC 单模态仍是主导信号**（平均 ARI=0.912），融合在 D1/D2/D3 上略低于 ATAC 单模态，说明 RNA 分支的噪声仍然对融合有轻微拖累
- **D5 上融合首次超越 ATAC 单模态**（0.962 vs 0.960），这是一个积极信号

### 5.4 设计选择的有效性验证

| 设计选择 | 变体 | Dataset 1 ARI | 结论 |
|---------|------|--------------|------|
| **双图并行 (v7a)** | 单图 (v6.1) | 0.772 → **0.815** | **有效，+0.043** |
| 非对称 CMA | 双向对称 CMA | 0.76 → 0.74 | 非对称设计有效保护 ATAC |
| 跨模态重建 | 仅自重建 (v9) | 0.24 | 跨模态重建是防止坍塌的必要条件 |
| Stop-gradient 对齐 | 直接 MSE 对齐 | 0.76 → 0.68 | Stop-gradient 防止对称梯度坍塌 |
| PCA 维度 | PCA 100 / PCA 300 | 0.75 / 0.37 | PCA 50 是最佳维度 |
| 端到端投影 | 原始 RNA → MLP | 0.06 | 端到端投影导致训练崩溃 |
| 加权融合 | concat([α*z_r, z_a]) | α=1.0 最优 | 简单拼接优于加权融合 |

### 5.5 总结与展望

STAMP v7a 通过引入**双图并行编码器**（空间图 + 特征相似性图），在保持 v6.1 核心架构（非对称 CMA、跨模态重建）不变的前提下，将平均 ARI 从 **0.894 提升至 0.903**。该方法在模态质量不均衡的空间 RNA+ATAC 场景中展现出稳定且优异的性能。

**未来改进方向**：
1. **自适应特征图邻居数**：当前固定 k=15，可根据局部密度自适应调整（如 D4 可能需要更小的 k 以避免过平滑）
2. **RNA 分支增强**：当前 RNA 单分支平均 ARI 仅 0.35，仍是最大瓶颈。引入 STAGATE 式的深度预训练（带空间约束）可能进一步提升
3. **门控修复**：在 v7a 基础上叠加一个保守的自适应融合门控（初始化 confidence≈0.3），有望在 D1/D2 上获得额外提升

---

*报告版本：v7a | 生成日期：2026-04-14*
*代码仓库：/data/lvyongji/Assignment5/code*
*结果数据：/data/lvyongji/Assignment5/Fig2_Benchmark/Processed_Simulated_Data/*
*Baseline 备份：/data/lvyongji/Assignment5/baseline_v6.1/*
